ext {
    // SDK
    compileSdkVersion = 29
    minSdkVersion = 21
    buildToolsVersion = '29.0.2'

    // Gradle
    gradleVersion = '4.1.1'

    // Google Play Services
    googlePlayServicesVersion = '4.3.2'

    // AppCompat Version
    appcompatVersion = '1.2.0'

    // Testing
    testingLegacySupportVersion = '1.0.0'
    testingJunitVersion = '4.13.1'
    testingAndroidxJunitVersion = '1.1.2'
    testingEspressoVersion = '3.3.0'

    // Design
    designConstraintLayoutVersion = '2.0.4'
    designRecyclerViewVersion = '1.1.0'
    designCardViewVersion = '1.0.0'
    designMaterialVersion = '1.3.0-alpha03'

    // Firebase
    firebaseAuthVersion = '20.0.1'

    // CircleImageView
    circleImageViewVersion = '3.1.0'

    // Kotlin
    kotlinVersion = '1.4.20'

    // Coroutines
    coroutinesVersion = '1.4.0'

    // Anko
    ankoVersion = '0.10.8'

    // PullToRefresh
    p2rVersion = '1.2.0'

    // RxJava2 + RxAndroid
    rxJavaVersion = '2.2.20'
    rxAndroidVersion = '2.1.1'

    // Retrofit2 + Converter_GSON + Adapter_RxJava2
    retrofitVersion = '2.9.0'

    // OkHTTP3
    okHttpVersion = '4.9.0'

    // Jetpack
    androidxPreferenceVersion = '1.1.1'

    // Glide
    glideVersion = '4.11.0'

    // Lifecycle Extensions
    lifecycleExtensionsVersion = '2.2.0'

    // Dagger2
    daggerVersion = '2.30'

    // Moxy-X
    moxyVersion = '2.2.0'

    // Room
    roomVersion = '2.2.5'

    // Cicerone
    ciceroneVersion = '5.0.0'

    // Shimmer
    shimmerVersion = '0.5.0'
}